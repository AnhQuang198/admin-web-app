import React from 'react';
import PropTypes from 'prop-types';
import './style.scss';

ShopAddress.propTypes = {
    
};

function ShopAddress(props:any) {
    return (
        <div>
            Shop address
        </div>
    );
}

export default ShopAddress;