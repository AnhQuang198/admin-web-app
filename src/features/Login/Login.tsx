import React from 'react';
import PropTypes from 'prop-types';

Login.propTypes = {
    
};

function Login(props: any) {
    return (
        <div>
            Login screen
        </div>
    );
}

export default Login;